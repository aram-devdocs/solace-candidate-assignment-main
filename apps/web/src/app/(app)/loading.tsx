import { SkeletonAdvocateListTemplate } from "@repo/ui";

/**
 * Loading UI for route transitions
 *
 * Displays skeleton loading state when navigating between pages
 */
export default function Loading() {
  return <SkeletonAdvocateListTemplate />;
}
