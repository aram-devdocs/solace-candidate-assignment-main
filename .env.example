# Database Configuration
# Neon serverless PostgreSQL connection string
# For local development using Neon proxy (requires docker-compose up):
DATABASE_URL=postgres://postgres:postgres@db.localtest.me:5432/main

# For production (use your Neon cloud connection string):
# DATABASE_URL=postgres://username:password@your-neon-host.neon.tech/database?sslmode=require

# Application Environment
NODE_ENV=development

# Next.js Configuration (optional)
# NEXT_PUBLIC_API_URL=http://localhost:3000
