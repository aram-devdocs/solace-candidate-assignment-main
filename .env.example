# Database Configuration
# Neon serverless PostgreSQL connection string
# For local development using Neon proxy (requires docker-compose up):
DATABASE_URL=postgres://postgres:postgres@db.localtest.me:5432/main

# For production (use your Neon cloud connection string):
# DATABASE_URL=postgres://username:password@your-neon-host.neon.tech/database?sslmode=require

# Application Environment
NODE_ENV=development

# Redis Configuration
# For local development (requires docker-compose up):
REDIS_URL=redis://localhost:6379

# For production (use your Redis cloud connection string):
# REDIS_URL=redis://username:password@your-redis-host:6379

# Database Seeding Configuration
# Number of advocate records to seed in the database (default: 100)
# Adjust this value to test different dataset sizes
# - Small dataset (10-50): Quick tests and basic development
# - Medium dataset (100-500): Realistic pagination and filtering tests
# - Large dataset (1000+): Performance testing and stress testing
SEED_COUNT=100

# Client-side Caching Configuration
# Maximum records to fetch on initial page load (NEXT_PUBLIC_ prefix makes it available to client)
NEXT_PUBLIC_MAX_INITIAL_FETCH=500

# Maximum records to cache client-side (prevents memory issues)
MAX_CLIENT_CACHE=2000

# Background fetch batch size for progressive loading
BACKGROUND_BATCH_SIZE=100

# Next.js Configuration (optional)
# NEXT_PUBLIC_API_URL=http://localhost:3000
